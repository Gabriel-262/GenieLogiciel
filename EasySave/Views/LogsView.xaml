<UserControl x:Class="EasySave.Views.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" Margin="0,0,0,4">
            <TextBlock Text="Journaux" Style="{StaticResource H1}"/>
            <TextBlock Style="{StaticResource Subtitle}">
                <Run Text="Dossier : "/>
                <Run Text="{Binding LogDirectory, Mode=OneWay}"/>
            </TextBlock>
        </StackPanel>

        <!-- Toolbar -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,16,0,12">
            <Button Content="↻  Rafraîchir"
                    Command="{Binding RefreshCommand}"/>
            <Button Content="Ouvrir le dossier"
                    Command="{Binding OpenLogFolderCommand}"/>
        </StackPanel>

        <!-- Main split: files left, entries right -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- File list -->
            <Border Grid.Column="0" Style="{StaticResource Card}" Padding="0" Margin="0,0,12,0">
                <DockPanel LastChildFill="True">
                    <Border DockPanel.Dock="Top"
                            BorderBrush="{DynamicResource BorderBrush}"
                            BorderThickness="0,0,0,1"
                            Padding="14,10">
                        <TextBlock Text="FICHIERS" Style="{StaticResource Label}"/>
                    </Border>
                    <ListBox ItemsSource="{Binding Files}"
                             SelectedItem="{Binding SelectedFile, Mode=TwoWay}"
                             BorderThickness="0"
                             Background="Transparent"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="14,10"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Bd"
                                                    Background="{TemplateBinding Background}"
                                                    BorderBrush="{TemplateBinding BorderBrush}"
                                                    BorderThickness="{TemplateBinding BorderThickness}"
                                                    Padding="{TemplateBinding Padding}">
                                                <ContentPresenter HorizontalAlignment="Stretch"/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource SurfaceAltBrush}"/>
                                                </Trigger>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Bd" Property="Background" Value="{DynamicResource AccentBrush}"/>
                                                    <Setter Property="Foreground" Value="{DynamicResource AccentForegroundBrush}"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding Name}" FontWeight="SemiBold"/>
                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                        <TextBlock Text="{Binding LastWriteTime, StringFormat=g}"
                                                   Opacity="0.75" FontSize="11"/>
                                        <TextBlock Text="  ·  " Opacity="0.5" FontSize="11"/>
                                        <TextBlock Text="{Binding SizeDisplay}" Opacity="0.75" FontSize="11"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </DockPanel>
            </Border>

            <!-- Entries grid -->
            <Border Grid.Column="1" Style="{StaticResource Card}" Padding="0">
                <DataGrid ItemsSource="{Binding Entries}"
                          BorderThickness="0"
                          HeadersVisibility="Column"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Timestamp, StringFormat=g}" Width="140"/>
                        <DataGridTextColumn Header="Job"  Binding="{Binding JobId}"      Width="50"/>
                        <DataGridTextColumn Header="Nom"  Binding="{Binding BackupName}" Width="140"/>
                        <DataGridTextColumn Header="Action" Binding="{Binding Action}" Width="120"/>
                        <DataGridTextColumn Header="Source"      Binding="{Binding SourceFilePath}"      Width="2*"/>
                        <DataGridTextColumn Header="Destination" Binding="{Binding DestinationFilePath}" Width="2*"/>
                        <DataGridTextColumn Header="Taille (o)"  Binding="{Binding FileSizeBytes}"       Width="90"/>
                        <DataGridTextColumn Header="Transfert (ms)" Binding="{Binding TransferTimeMs}"   Width="100"/>
                        <DataGridTextColumn Header="Crypto (ms)" Binding="{Binding CryptoTimeMs}"        Width="90"/>
                        <DataGridTextColumn Header="Thread" Binding="{Binding ThreadId}" Width="60"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>

        <!-- Status bar -->
        <Border Grid.Row="3"
                BorderBrush="{DynamicResource BorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="0,10,0,0"
                Margin="0,12,0,0">
            <TextBlock Text="{Binding StatusMessage}" Style="{StaticResource Muted}"/>
        </Border>
    </Grid>
</UserControl>
