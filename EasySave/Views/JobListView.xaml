<UserControl x:Class="EasySave.Views.JobListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Backup jobs" Style="{StaticResource H1}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Add"     Style="{StaticResource PrimaryButton}" Click="Add_Click"/>
            <Button Content="Edit"    Click="Edit_Click"/>
            <Button Content="Delete"  Style="{StaticResource DangerButton}" Click="Delete_Click"/>
            <Button Content="Execute" Click="Execute_Click"/>
            <Button Content="Execute all" Click="ExecuteAll_Click"/>
            <Button Content="Refresh" Click="Refresh_Click"/>
        </StackPanel>

        <Border Grid.Row="2" Style="{StaticResource Card}" Padding="0">
            <DataGrid x:Name="Grid" ItemsSource="{Binding JobList.Jobs}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID"     Binding="{Binding Id}"         Width="60"/>
                    <DataGridTextColumn Header="Name"   Binding="{Binding Name}"       Width="*"/>
                    <DataGridTextColumn Header="Type"   Binding="{Binding Type}"       Width="120"/>
                    <DataGridTextColumn Header="Source" Binding="{Binding SourcePath}" Width="2*"/>
                    <DataGridTextColumn Header="Target" Binding="{Binding TargetPath}" Width="2*"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
